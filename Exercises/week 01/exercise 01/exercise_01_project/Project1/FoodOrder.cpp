#include "FoodOrder.h"


// Use sstream to read from a file and ignore() to ignores comma when its a double or last char
namespace seneca {
	istream& FoodOrder::read(istream& in)
	{
		if (in.good()) {

		}
	}
}